name: Scan Neburst2

on:
  workflow_dispatch:
  schedule:
    - cron: '10 6,18 * * *'   # 时间和其它错开

concurrency:
  group: scan-neburst2
  cancel-in-progress: false

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt

      - name: Pull state
        env:
          TOKEN: ${{ secrets.RESULT_REPO_TOKEN }}
        run: |
          git clone https://x-access-token:${TOKEN}@github.com/zerogege/proxyip.git result_repo
          mkdir -p state
          cp result_repo/state/*.txt state/ 2>/dev/null || true
          cp result_repo/*.txt ./ 2>/dev/null || true

      - name: Run Scan
        env:
          CUSTOM_CF_DOMAIN: ${{ secrets.CUSTOM_CF_DOMAIN }}
        run: python final_scan.py "200772" "Neburst2" "1-65535"

      - name: Push
        env:
          TOKEN: ${{ secrets.RESULT_REPO_TOKEN }}
        run: |
          cd result_repo
          mkdir -p state

          # 只复制当前任务自己的文件，不再用通配符
          cp ../Neburst2.txt ./ 2>/dev/null || true
          cp ../state/scanned_ports_Neburst2.txt state/ 2>/dev/null || true
          cp ../state/pending_Neburst2.txt state/ 2>/dev/null || true

          git config user.email "bot@users.noreply.github.com"
          git config user.name "bot"

          # 分开 add 并容错：首次运行时 pending 文件可能还不存在
          git add Neburst2.txt || true
          git add state/scanned_ports_Neburst2.txt || true
          git add state/pending_Neburst2.txt || true

          if git diff --staged --quiet; then
            echo "no change"
          else
            git commit -m "Neburst2 update"

            # 自动 rebase，冲突时用 sort -u 合并
            for i in {1..5}; do
              if git pull --rebase origin main; then
                break
              fi
              if [ -d .git/rebase-merge ] || [ -d .git/rebase-apply ]; then
                CONFLICTS=$(git diff --name-only --diff-filter=U)
                if [ -n "$CONFLICTS" ]; then
                  for f in $CONFLICTS; do
                    sort -u "$f" -o "$f"
                    git add "$f"
                  done
                fi
                GIT_EDITOR=true git rebase --continue
              else
                sleep 2
              fi
            done

            git push origin main
          fi

      - name: Backup to GitLab
        if: always()
        env:
          GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}
        run: |
          git clone https://oauth2:${GITLAB_TOKEN}@gitlab.com/7969521/proxyip-backup.git gitlab_repo
          cp -r result_repo/*.txt gitlab_repo/ 2>/dev/null || true
          mkdir -p gitlab_repo/state
          cp result_repo/state/*.txt gitlab_repo/state/ 2>/dev/null || true
          cd gitlab_repo
          git config user.email "bot@backup.com"
          git config user.name "bot"
          git add -A
          if git diff --staged --quiet; then
            echo "GitLab: no change"
          else
            git commit -m "neburst2 backup"
            git pull --rebase 2>/dev/null || true
            git push
          fi

      - name: Notify TG
        if: always()
        run: |
          COUNT=$(cat count.txt 2>/dev/null || echo "?")
          if [ "${{ job.status }}" = "success" ]; then
            MSG="✅ Neburst2 扫描完成%0A本次新增: ${COUNT} 个"
          else
            MSG="❌ Neburst2 扫描失败%0A状态: ${{ job.status }}"
          fi
          curl "https://api.telegram.org/bot${{ secrets.TG_TOKEN }}/sendMessage" \
            -d chat_id=${{ secrets.TG_CHAT_ID }} \
            -d text="${MSG}"
